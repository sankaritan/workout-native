# Cloudflare Pages ignore file
# This overrides .gitignore for deployments
# We want to deploy the web-build output, so we DON'T ignore it here

# Only ignore development files
node_modules/
.expo/
dist/
*.log
.DS_Store
